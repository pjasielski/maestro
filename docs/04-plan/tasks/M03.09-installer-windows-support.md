# Task M03.09: Installer hotfix — Windows story
**ID:** M03.09  (matches ROADMAP # column and filename `M03.09-installer-windows-support.md`)
**Milestone:** M03
**Status:** todo
**Priority:** high
**Assigned:** unassigned
**Blocked by:** —
**Effort:** S
**Created:** 2026-07-18
**Completed:**

## Description
Field report (2026-07-18): Windows user could not install — install.sh is bash-only, nothing runs in PowerShell. Interim fix until the M05 Python CLI replaces the installer: document the supported Windows paths (Git Bash, WSL) prominently in README + docs/installation.md, with exact commands; evaluate whether a minimal `install.ps1` is worth maintaining or whether docs + M05 timeline suffice.

## Acceptance Criteria
- [ ] README + docs/installation.md have a Windows section (Git Bash / WSL, copy-paste commands)
- [ ] Explicit note: PowerShell not supported until the Python CLI (M05)
- [ ] Decision recorded: install.ps1 yes/no (if yes, minimal parity: download + questions + scaffold)

## Files
- `README.md`, `docs/installation.md`, optionally `install.ps1`

## Notes
M05.02 (`maestro init` via uvx) is the real cross-platform fix; don't over-invest here.
